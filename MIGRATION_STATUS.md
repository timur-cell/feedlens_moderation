# FeedLens Rails Migration — Acceptance Status

Live checklist of the acceptance criteria. Updated whenever a criterion flips.

| # | Criterion | Status | Notes |
|---|---|---|---|
| 1 | `docker compose up` from fresh clone boots stack; `rails db:setup` migrates + seeds; steps proven in clean checkout per rails/README.md | ✅ done | verified in a clean checkout (/tmp clone, empty volumes): all 5 services healthy, container DB seeded 81 rules / 22 lists / 8 templates / settings / admin; `bin/rails db:setup` also verified from the clean checkout against a bare Postgres |
| 2 | Schema: all Convex tables with listed indexes; model validations match Convex constraints | ✅ done | 14 tables, epoch-ms bigints, jsonb, all Convex indexes + uniques; 70 model specs green |
| 3 | Rule engine parity + golden-fixture suite (≥30 fixtures, expectations generated from TS via bun), green in RSpec | ✅ done | 66 golden fixtures generated by executing the real TS evaluators with bun; 59 distinct rules fired; 112 service specs green |
| 4 | AI integrations (Claude vision + param scan, GPT-4o fallback) as job/service classes, defensive parsing parity, WebMock/VCR tests | ✅ done (verified mocked only) | Vision/param-scan/LLM-verifier/suggesters/remediation with TS prompt+parsing parity; WebMock specs cover request shapes and all defensive-parsing cases. **No AI keys in this env and api.openai.com is proxy-blocked** → live smoke test not possible here |
| 5 | Implio submission job, payload parity, `IMPLIO_STUB=true` default ON | ✅ done | exact submitToImplio payload fixture-compared in specs; stub default ON (zero HTTP asserted when unset) |
| 6 | `POST /api/push-flagged` (contract+dedup+constant-time key) and `GET /image-proxy` (hostname validation) with request specs incl. auth failures + SSRF attempts | ✅ done | constant-time digest compare; TS-parity dedup/create/update; 6 SSRF bypass cases + auth failures covered in request specs |
| 7 | Full REST API behind Devise auth + role checks (admin-only → 403 for moderators) | ✅ done | 277 request-spec examples: 401 unauth/disabled, 403 on every admin endpoint, viewer-passes parity |
| 8 | All 14 pages work against Rails API (polling where realtime was); Playwright e2e (login, every page, moderate-by-id flow, rules CRUD) headless against compose stack | ✅ done | Playwright run headless against the fresh-clone compose stack: **23/23 assertions** — login, every page, rules CRUD round-trip, full moderate-by-id flow (fetch → moderate → persisted result). **www.jamesedition.com is blocked by this sandbox's egress proxy** → the flow runs against the mock JE API from docker-compose.e2e.yml; live-JE fetch is implemented + WebMock-tested but unverifiable from here |
| 9 | `bundle exec rspec` green; `rubocop` clean (omakase); frontend `tsc --noEmit` clean | ✅ done | re-verified in the clean checkout: 586 examples 0 failures (incl. the convex:import spec); rubocop 160 files no offenses; tsc clean (app + root projects); vite build green |
| 10 | MIGRATION_STATUS.md all-green + HANDOFF.md (env vars, Convex import rake task, not-migrated list, behavioral differences) | ✅ done | HANDOFF.md at repo root covers env vars, `convex:import`, not-migrated items with replacements, and 12 justified behavioral differences |

## Environment notes / deviations

- **Seed count**: the TS seed (`convex/seedAllRules.ts`) contains **81** rules, not the 79 the
  spec mentions; the TS code wins, so seeds insert 81 (one of them, `too_low_price_430k`,
  ships disabled — also TS parity).
- Docker Hub anonymous pulls are rate-limited in this environment; base images use
  `mirror.gcr.io/library/*` (byte-identical mirrors of Docker Hub official images);
  override with `BASE_REGISTRY=docker.io`.
- Sandbox egress blocks `www.jamesedition.com` / `img.jamesedition.com` and `api.openai.com`;
  live integrations against those hosts are implemented and unit-tested via WebMock but can
  only be smoke-tested outside this environment.
- No `ANTHROPIC_API_KEY` / `OPENAI_API_KEY` available here → AI integrations are
  "verified mocked only" (criterion 4 wording).
