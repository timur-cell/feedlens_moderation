// Single source of truth for backend (Convex) code — imported by
// convex/ViktorSpacesEmail.ts (and any other convex/*.ts). The frontend
// keeps its own copy in src/lib/constants.ts because the Convex and Vite
// build trees cannot share a module across the convex/ <-> src/ boundary.
export const APP_NAME = "FeedLens";
